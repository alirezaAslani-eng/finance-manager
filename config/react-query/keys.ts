const keys = {
  userInfo: {
    all: ["user-info"],
  },
  recntTransactions: {
    all: ["recent-transactions"],
  },
  allTransactions: {
    all: ["transactions"],
  },
} as const;

export default keys;
