import { styled } from "@mui/material";
import Link from "next/link";

const BottomNavigationLink = styled(Link)({
  display: "block",
  width: "100%",
  height: "100%",
});

export default BottomNavigationLink;
